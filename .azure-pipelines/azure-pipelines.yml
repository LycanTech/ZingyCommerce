# ============================================================
# DEPRECATED — This pipeline has been replaced by:
#   app-pipeline.yml       — builds Docker images (triggers on services/**)
#   infra-pipeline.yml     — Terraform plan/apply (triggers on infrastructure/**)
#   bootstrap-pipeline.yml — one-time cluster setup (manual only)
#
# This file is kept for reference only. Do not run it.
# ============================================================

trigger: none

pool:
  vmImage: ubuntu-latest

steps:
  - bash: |
      echo "This pipeline is deprecated."
      echo "Use app-pipeline.yml, infra-pipeline.yml, or bootstrap-pipeline.yml instead."
      exit 0
    displayName: 'Deprecation notice'
