# The JWT secret is expected to already exist in the cluster
# (created once via: kubectl create secret generic zingycommerce-secrets
#                      --from-literal=jwt-secret="$(openssl rand -hex 32)")
# This template is intentionally empty — we reference it by name in deployments.
# Do NOT store real secrets in Helm values or Git.
